Website template mockups for the branding-style pickers (Real / Animated / Mixed).

Each design uses two files in this folder:
  {slug}-thumb.{jpg|png}  — shown in the gallery grid
  {slug}.{jpg|png}        — full-size image opened by the zoom button

Configured in lib/schema/templateGalleries.ts (grouped by branding_style answer).
