/** Nested in `answers` JSON alongside legacy export keys; holds the live form state. */
export const FOUNDATION_APP_ANSWERS_KEY = "_foundationOnboardAnswers";

/** Last saved per-section visible question completion (for reporting / audits); not hydrated into form state. */
export const FOUNDATION_SECTION_PROGRESS_KEY = "_foundationSectionQuestionProgress";
